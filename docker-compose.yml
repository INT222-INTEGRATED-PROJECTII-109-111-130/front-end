version: '2'
services:
    front-docker:
        container_name: "frontend"
        image: ghcr.io/violet-evergardenz/int222-frontend:latest
        ports:
            - "80:80"
            - "443:443"
        restart: on-failure